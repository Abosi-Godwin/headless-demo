export function AnnouncementBar() {
    return (
        <div className="w-full bg-foreground text-background font-mono text-xs tracking-widest uppercase py-2 text-center">
            Standard shipping included on every issue over $75
        </div>
    );
}